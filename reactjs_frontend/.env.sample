# Optional: Override the API base used by the React frontend.
# If not set, the app will default to the cloud preview URL below.
# To use locally, set: REACT_APP_API_BASE=http://localhost:3001
REACT_APP_API_BASE=https://vscode-internal-23153-beta.beta01.cloud.kavia.ai:3001
